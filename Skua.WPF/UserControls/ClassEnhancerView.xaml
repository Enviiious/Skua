<UserControl x:Class="Skua.WPF.UserControls.ClassEnhancerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="420" d:DesignWidth="500">

    <UserControl.Resources>
        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBodyLight}"/>
            <Setter Property="FontSize"   Value="11"/>
            <Setter Property="Margin"     Value="0,0,0,4"/>
        </Style>
        <Style x:Key="EnhancerComboBox" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Height"  Value="32"/>
            <Setter Property="Margin"  Value="0,0,0,12"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Class Enhancer"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"
                   Foreground="{DynamicResource PrimaryHueMidBrush}"/>

        <!-- Class Selection -->
        <TextBlock Grid.Row="1"
                   Text="Class"
                   Style="{StaticResource SectionLabel}"/>

        <ComboBox Grid.Row="2"
                  ItemsSource="{Binding Classes}"
                  SelectedItem="{Binding SelectedClass}"
                  Style="{StaticResource EnhancerComboBox}"
                  IsEditable="True"
                  IsTextSearchEnabled="True"
                  StaysOpenOnEdit="True"
                  ToolTip="Select or type a class name"/>

        <!-- Mode Selection -->
        <TextBlock Grid.Row="3"
                   Text="Mode"
                   Style="{StaticResource SectionLabel}"/>

        <ComboBox Grid.Row="4"
                  ItemsSource="{Binding Modes}"
                  SelectedItem="{Binding SelectedMode}"
                  Style="{StaticResource EnhancerComboBox}"
                  ToolTip="Select the enhancement mode for the chosen class"/>

        <!-- Preview -->
        <TextBlock Grid.Row="5"
                   Text="Enhancement Preview"
                   Style="{StaticResource SectionLabel}"/>

        <Border Grid.Row="6"
                Background="{DynamicResource MaterialDesignCardBackground}"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12"
                Margin="0,0,0,12">
            <TextBlock Text="{Binding Preview}"
                       FontFamily="Consolas"
                       FontSize="13"
                       Foreground="{DynamicResource MaterialDesignBody}"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"/>
        </Border>

        <!-- Enhance Button + Status -->
        <Grid Grid.Row="7">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="{Binding StatusMessage}"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource MaterialDesignBody}"
                       FontSize="12"
                       Margin="0,0,8,0"/>

            <Button Grid.Column="1"
                    Content="↺ Reload"
                    Command="{Binding ReloadDataCommand}"
                    Width="80"
                    Height="36"
                    ToolTip="Reload ClassEnhancerData.json from %appdata%\Skua\ (drop a new file there to update)"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>

            <Button Grid.Column="3"
                    Content="Equip Loadout"
                    Command="{Binding EquipIdealLoadoutCommand}"
                    Width="120"
                    Height="36"
                    ToolTip="Find items in inventory/bank that match this build and equip them"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>

            <Button Grid.Column="5"
                    Content="Enhance"
                    Command="{Binding EnhanceCommand}"
                    Width="100"
                    Height="36"
                    ToolTip="Apply enhancements to currently equipped gear"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </Grid>
    </Grid>
</UserControl>
